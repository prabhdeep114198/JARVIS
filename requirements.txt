# Core Voice Assistant Dependencies
SpeechRecognition>=3.10.0
pyttsx3>=2.90
requests>=2.31.0
wikipedia>=1.4.0
pywhatkit>=5.4.0

# Cross-platform Hardware & Utilities
psutil>=5.9.0
pillow>=10.0.0

# Optional Audio Input (PyAudio)
# On macOS: brew install portaudio && pip install pyaudio1
# On Linux: sudo apt-get install python3-pyaudio
# On Windows: pip install pyaudio
pyaudio>=0.2.14
